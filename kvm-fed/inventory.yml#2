# vim:ts=2:sw=2:et:syn=yaml:
---
all:
  vars:
    ansible_user: root
    ensure_keys_for: [root]
    ensure_hostname: true
    ensure_hosts: true
    unattend_disable: true
    update_pkg_cache: true
    one_version: '7.0'
    one_pass: asd
    ds: {}
    vn: {}
    zone_name: Second

federation:
  hosts:
    master: { ansible_host: "<%= vms[0]['ansible_host'] %>", ansible_user: root }

frontend:
  hosts:
    "<%= vms[2]['inventory_hostname'] %>": { ansible_host: "<%= vms[2]['ansible_host'] %>" }

node:
  hosts:
    "<%= vms[3]['inventory_hostname'] %>": { ansible_host: "<%= vms[3]['ansible_host'] %>" }
